---
slug: /
sidebar_position: 1
hide_table_of_contents: true
pagination_next: null
---

import Link from '@docusaurus/Link';
import styles from './intro.module.css';

# Overview

## Hi there! 👋

This guide will help you get the most out of your Tether theme.

Browse the categories below to find exactly what you need.

<div className="margin-top--lg">
  <div className="row">
    <div className="col col--6 margin-bottom--lg">
      <Link to="/category/getting-started" className={`${styles.introCard} card padding--lg`}>
        <h2 className={`text--truncate ${styles.introCardTitle}`}>📄️ Getting started</h2>
        <p className={`text--truncate ${styles.introCardDescription}`}>Get up and running with Tether and Shopify.</p>
      </Link>
    </div>

    <div className="col col--6 margin-bottom--lg">
      <Link to="/category/theme-settings" className={`${styles.introCard} card padding--lg`}>
        <h2 className={`text--truncate ${styles.introCardTitle}`}>📄️ Theme settings</h2>
        <p className={`text--truncate ${styles.introCardDescription}`}>Theme settings let you customize the overall look and feel of your Shopify store. They apply across your whole site to keep everything consistent.</p>
      </Link>
    </div>

    <div className="col col--6 margin-bottom--lg">
      <Link to="/category/sections" className={`${styles.introCard} card padding--lg`}>
        <h2 className={`text--truncate ${styles.introCardTitle}`}>📄️ Sections</h2>
        <p className={`text--truncate ${styles.introCardDescription}`}>Sections are customizable content areas within a Shopify theme. They structure the layout of your pages. They can be added, removed, or rearranged in the theme editor.</p>
      </Link>
    </div>

    <div className="col col--6 margin-bottom--lg">
      <Link to="/category/blocks" className={`${styles.introCard} card padding--lg`}>
        <h2 className={`text--truncate ${styles.introCardTitle}`}>📄️ Blocks</h2>
        <p className={`text--truncate ${styles.introCardDescription}`}>Blocks are individual content elements that make up parts of a Section layout. Each block controls a specific piece of content, giving you detailed control over how your store looks and functions.</p>
      </Link>
    </div>
  </div>
</div>
